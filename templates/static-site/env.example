# Static Site Configuration
# Copy this file to .env and update with your values

# Comma-separated list of production domains where this site will be deployed
# These domains will be detected as static environments by StaticLink component
STATIC_SITE_DOMAINS=yourdomain.com,www.yourdomain.com

# Base URL for the static site (used in build scripts and sitemap generation)
STATIC_SITE_BASE_URL=https://yourdomain.com

# Vite environment variables (for client-side access)
# These will be injected during build
VITE_STATIC_SITE_DOMAINS=yourdomain.com,www.yourdomain.com

# Development Configuration
NODE_ENV=development
PORT=3000

