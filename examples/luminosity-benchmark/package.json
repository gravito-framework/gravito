{
  "name": "luminosity-benchmark",
  "version": "1.0.0",
  "private": true,
  "description": "Firepower showcase for Gravito Luminosity Sitemap Engine",
  "type": "module",
  "scripts": {
    "start": "bun run src/index.ts",
    "seed": "bun run src/seed.ts",
    "benchmark": "bun run src/benchmark.ts",
    "clean": "rm -rf dist-sitemaps/* && rm -f db.sqlite"
  },
  "dependencies": {
    "@gravito/constellation": "workspace:*",
    "@gravito/luminosity": "workspace:*",
    "better-sqlite3": "^11.0.0",
    "faker": "^6.6.6",
    "cli-table3": "^0.6.3",
    "chalk": "^5.3.0"
  },
  "devDependencies": {
    "bun-types": "latest",
    "typescript": "^5.0.0",
    "@types/better-sqlite3": "^7.6.0"
  }
}
